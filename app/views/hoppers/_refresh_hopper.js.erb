$(".hopper[data-hopper=<%= hopper.id %>]").replaceWith("<%= escape_javascript(render 'hoppers/hopper', hopper: hopper) %>");

<% unless current_user.pinned_hopper.nil? %>
  $(".pinned-hopper").replaceWith("<%= escape_javascript(render 'hoppers/pinned_hopper', hopper: hopper) %>");
<% end %>
